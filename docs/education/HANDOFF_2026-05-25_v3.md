# Task Hand-off Document

**Date**: 2026-05-25  
**Current Time**: 12:05 PM KST  
**Domain**: Harness physical AI / Education Consulting Platform  
**Target Audience**: Incoming LLM Agent / Developer  

---

## 1. Executive Summary & Context
We are actively building the Harness Physical AI / Education Consulting Platform. The primary objective is to build a high-fidelity data collection, filtering, and decision platform that captures global anxieties (parents' AI education anxiety and workers' AI job replacement anxiety) across **20 major global languages** and maps them to a strategic "FOMO-to-Solution Lookup Table".

---

## 2. Completed Milestones (Handoff State)

### A. Core Frontend Fix & Routing Hardening
* **Problem**: The Vite/React frontend was crashing with compile errors because `TopBar.tsx` was modified to support tabbed multi-page navigation, but the parent `App.tsx` did not have subpage routing states or proper property contracts.
* **Resolution**: 
  * Implemented fully typed React routing state inside `App.tsx`.
  * Imported and successfully integrated all subpages:
    * **Home (Dashboard)**: Core metrics and live crawler stats.
    * **결재 (ApprovalsPage)**: Operational approvals inbox.
    * **회의실 (ConferenceRoomPage)**: Internal team collaboration notes.
    * **비용 분석기 (CostsPage)**: Real-time LLM cost metrics.
    * **토큰 사용량 (TokenUsagePage)**: Token consumption monitoring.
    * **회의록 (MeetingNotesPage)**: Historical minutes viewer.
    * **설정 (SettingsPage)**: User settings and role switching.
    * **AR현황 (Action Required Status)**: Live table of pending and resolved items.
  * **Build Verification**: Ran `npm run build` inside `harness-os/frontend`. Compiles perfectly without warnings or errors (`tsc -b && vite build` succeeded). HMR (Hot Module Replacement) is fully operational at `http://127.0.0.1:5173/`.

### B. Dynamic Data Ingestion Monitor
* **Backend API Integration**: Updated `harness-os/backend/main.py` (`/api/dashboard/advanced` endpoint) to query live statistical status from the `raw_signals` table in PostgreSQL.
* **Frontend Panel**: Embedded the **"글로벌 20대 다국어 데이터 수집 및 연관성 필터링 현황"** monitor directly into the frontend. It dynamically reflects:
  * **Pending Count**: Original unclassified raw signals.
  * **Passed Count**: Signals vetted and verified by Ollama as highly relevant.
  * **Failed Count**: Unrelated noise filtered out by Ollama.
  * **Recent Ingested Topics**: A dynamic list of the 5 most recently discovered global signal topics with their filtration status.

### C. Exhaustive Multilingual Keyword Expansion
* Added comprehensive query arrays supporting **20 global languages** (English, Spanish, French, German, Japanese, Chinese, Portuguese, Italian, Russian, Arabic, Hindi, Indonesian, Turkish, Vietnamese, Dutch, Polish, Swedish, Hebrew, Korean).
* **UAE & Israeli Hardening**: Integrated native Arabic script queries targeting UAE-specific automation (Dubai, Abu Dhabi, UAE job automation) and native Hebrew script queries targeting Israeli education contexts in:
  1. Academic Gather: `scripts/run_edu_deep_research.py`
  2. YouTube Gather: `scripts/run_edu_youtube_gather.py`
* **Test Validation**: Executed isolated gather tests on YouTube for Arabic (`الذكاء الاصطناعي في التعليم...`) and Hebrew (`חרדת הורים...`) search commands. Both successfully discovered matching target video items.

### D. Strategic Pivot Lookup Table
* Drafted the strategic pivot framework: [fomo_solution_lookup_strategy.md](file:///Users/juntae.park/.gemini/antigravity-cli/brain/9ee31bd8-688c-47f8-86c7-0f88d84e3289/fomo_solution_lookup_strategy.md).
* Outlined distinct anxiety Cases, matching action programs, standard validation paths, and a closed-loop "Out-of-Pattern Exception Loop."

---

## 3. Current System State
* **FastAPI Backend**: Running on `http://127.0.0.1:8000`.
* **Vite Frontend**: Running on `http://127.0.0.1:5173`.
* **Database**: PostgreSQL active, tables `raw_signals` are live.
* **Ollama Local Model**: `qwen2.5:1.5b` active on `11434`.

---

## 4. Immediate Next Steps for the Incoming Agent

### Step 1: Run Ingestion Batch for the Newly Added Arabic & Hebrew Queries
Execute the academic research gather script to crawl Semantic Scholar and arXiv for the new native Arabic and Hebrew papers:
```bash
# In workspace root
.venv/bin/python scripts/run_edu_deep_research.py --sources scholar,arxiv
```
*Note: The script has intelligent retry/backoff throttling to handle HTTP 429 rate limits, so let it run to completion safely.*

### Step 2: Trigger Local AI Filtering
Run the local Ollama filtration pipeline to classify the newly ingested raw signals into the database:
```bash
.venv/bin/python scripts/run_edu_filter.py --model "qwen2.5:1.5b" --limit 500
```
This will update the passed/failed/pending statistics reflected in the Frontend Ingestion Monitor dashboard.

### Step 3: Execute Cross-LLM Red Team Validation on the Lookup Table
Per `AGENTS.md` guidelines, high-impact strategy documents must undergo Cross-LLM Red Team validation (Gemini + Claude):
1. Review [fomo_solution_lookup_strategy.md](file:///Users/juntae.park/.gemini/antigravity-cli/brain/9ee31bd8-688c-47f8-86c7-0f88d84e3289/fomo_solution_lookup_strategy.md) with another model.
2. Obtain a `red_team_clear` verdict, verifying all regulatory constraints (South Korean Capital Market Act, False Advertising laws).

### Step 4: Verify Subpage Rendering in Browser
Open `http://127.0.0.1:5173/` and navigate through all 8 tabs to ensure there are no run-time rendering exceptions.

---

## 5. Critical Files Map
* `harness-os/frontend/src/App.tsx`: Main entry React app containing page routing state.
* `harness-os/frontend/src/components/TopBar.tsx`: Navigational top bar.
* `scripts/run_edu_deep_research.py`: Multilingual academic data crawler.
* `scripts/run_edu_youtube_gather.py`: Multilingual YouTube scraper.
* `fomo_solution_lookup_strategy.md`: Strategic pivot lookup framework.
