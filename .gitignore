.env
.venv/
__pycache__/
*.pyc
.DS_Store
*.log

# SSH keys — never commit
ssh *
*.pub

# Runtime state
runtime/
core/harness.db
summary

# Generated binary/HTML outputs
docs/*.pdf
docs/*.html
docs/reports/*.pdf
docs/reports/*.html

# Screenshots and media
*.png
*.jpg

# Claude internal
.claude/
harness-os/backend/passwords.json

# Local workspace temporary files
.antigravitycli/
scratch/
data/edu_research/evidence_index.json

# Runtime state (regenerated by prod processes; do not track — caused chronic dirty-tree)
docs/reports/ibkr_monitor_cache.json
docs/reports/ibkr_tws_positions.json
docs/reports/paper_trading_positions.json
docs/reports/paper_trading_log.jsonl
docs/reports/conference_room_stream.jsonl
docs/trading/trading_diary.jsonl
docs/reviews/openclaw_route_audit/openclaw_route_audit_daily.md
docs/reports/price_drop_alerts.jsonl
logs/lane_telemetry.jsonl

# Mass-regenerated outputs (2026-06-09: Mac Mini에 11,564개 누적 → 만성 dirty 원인)
# 정제 산출물의 system of record는 Notion (CLAUDE.md §10). git 추적 대상 아님.
docs/reports/qa/
docs/reviews/edu_pilot_simulations/
docs/reviews/conference_room_audit/
docs/reviews/trading_theme_bridge_precision/
docs/reviews/openclaw_route_audit/
data/collection_health_status.json

# 런타임 트레이딩/ops 상태 (prod 재생성)
docs/reports/ibkr_gateway_runtime_status.json
docs/reports/paper_trading_reset_report.json
docs/reports/paper_trading_reset_status.json
docs/reports/post_open_verification.json
docs/reports/trading_runtime_guard.json
docs/reports/resets/

# 프론트엔드 빌드 산출물 (npm run build로 frontend 루트에 in-place 생성 — 소스 아님)
# 소스 정적파일은 harness-os/frontend/public/ 에 있음. 루트의 것은 빌드 복사본.
harness-os/frontend/assets/
harness-os/frontend/edu-pilot-app.html
harness-os/frontend/parents-first.html
harness-os/frontend/favicon.svg
harness-os/frontend/icons.svg
harness-os/frontend/manifest.webmanifest

# 일별 raw 수집 데이터 (재수집 가능 — 파생물 evidence_bank/index가 실사용)
data/edu_research/*/naver_collected.json

# edu evidence 파생물 (run_pipeline/edu_daily/tier3-refine가 매 사이클 재생성 — 만성 dirty).
# 추적 해제 시 rollout에서 ff가 삭제할 수 있으니 배포는 백업→restore 동반(Codex). drift 가드도 제외.
data/edu_research/evidence_bank.json

# Unmatched-entity miner 산출물 (프로덕션 주1회 재생성 — system of record 아님)
docs/trading/universe_candidate_queue.json
docs/trading/UNIVERSE_CANDIDATE_QUEUE.md
docs/reports/approval_intake.jsonl

# 트레이딩 유니버스 빌드 산출물 (build_trading_universe.py가 seed+evidence로 매 사이클 재생성)
# 소스는 configs/trading/universe_seed.json + 수집 evidence. 추적 시 만성 dirty.
# 스케줄 트레이더는 run_trading_cycle.py가 실행 전 항상 재빌드(Codex 확인). drift 가드도 제외.
docs/trading/universe.json
